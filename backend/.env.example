# Server
PORT=3000
NODE_ENV=development

# Database (PostgreSQL)
DATABASE_URL="postgresql://user:password@localhost:5432/whatsapp_api?schema=public"

# Redis (for Bull queues and cache)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# Auth (opcional, JWT para proteger endpoints)
JWT_SECRET=your-secret-key-here

# Storage (para mídias)
MEDIA_STORAGE_PATH=./storage/media

# Baileys
BAILEYS_AUTH_PATH=./storage/auth

# CORS
CORS_ORIGIN=http://localhost:5173
